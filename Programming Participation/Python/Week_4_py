Explain how to use a list in a program

In a python program, lists can be used for many things, but are defined with square [] brackets. They can hold multiple items, of different types, and are stored in numerical order in their place, starting at 0 for the first item in the list, 1 for the second, and so forth. If I defined and added to a list named test, so that the 4th item in the list was a string "hello", I would call it using test[3], keeping in mind that list items start at 0. You can add to them using test.append(___), and you can remove items using del test[___], although if you wanted to return the removed value, lets say to systematically shrink and print a list at the same time, you would use test.pop(___), which would remove and return the value for the list at ___. (___) in this instance is a placeholder for whatever numerical value is inputted for the list position.